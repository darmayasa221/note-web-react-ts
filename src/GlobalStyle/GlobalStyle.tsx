import { css } from "@emotion/react";

export const Anchor = css({
  textDecoration: "none",
  color: "white",
  margin: 0,
  padding: 0,
  display: "block",
  cursor: "pointer",
});

export const Button = css({
  margin: 0,
  padding: 0,
  border: "none",
});
